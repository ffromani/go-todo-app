// Package controller implements the Controller part of the Model-View-Controller (MVC) pattern.
// Orchestrates the action requested by API, translate the API types to/from the internal types,
// manipulate the Model objects loading and saving them from the durable store.
package controller
